import cors from "@koa/cors";

const corsOpts = {
  exposeHeaders: [
    // pretty-ignore
    "Content-Length",
    "Content-Encoding",
    "Accept-Ranges",
    "Content-Range",
  ],
  credentials: true,
};

export default () => {
  return cors(corsOpts);
};
