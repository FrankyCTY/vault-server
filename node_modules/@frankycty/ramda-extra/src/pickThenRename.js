import { compose, pick } from "ramda";
import { renameKeys, isNilOrEmpty } from "ramda-adjunct";

export default (keyNames = [], keyNamesMap = {}, obj) => {
  const composedFn = compose(renameKeys(keyNamesMap), pick(keyNames));
  return isNilOrEmpty(obj) ? composedFn : composedFn(obj);
};
